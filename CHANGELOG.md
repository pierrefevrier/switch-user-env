Switch-user-env changes by release
=================================================

## 1.0.0 - First release

### Features:
- Change current $HOME (to make `cd` command without args redirect to target $HOME user)
- Load bash profile of target user (alias, prompt...)
